"""
backend/app/ml — Custom ML pipeline for OptiVue
================================================

Consumes MediaPipe landmarks and produces higher-level inferences
(posture class, fatigue score, gaze zone, etc.).

MediaPipe is the landmark detector.  This package sits *downstream* of it.
"""
