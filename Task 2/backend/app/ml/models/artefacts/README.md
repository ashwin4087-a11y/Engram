# This directory holds trained model artefacts.
# Generated by: python -m app.ml.training.train --data <csv_path>
#
# Expected files after training:
#   posture_classifier.ubj   — XGBoost binary model
#   feature_scaler.npz       — StandardScaler mean + std arrays
#   training_report.json     — Accuracy, per-class F1, confusion matrix
